import styled from 'styled-components';

const StyledWeather = styled.div`
    height: calc(100vh - 60px);

    .WT-weather__container {
        width: 100%;
        max-width: 928px;
        margin: 0 auto;
    }
`;

export default StyledWeather;
